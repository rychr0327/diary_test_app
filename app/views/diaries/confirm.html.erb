<!DOCTYPE>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <link rel="stylesheet" href="/assets/stylesheets/normalize.css">
    <link rel="stylesheet" href="/assets/stylesheets/style.css">
  </head>
  <body>
    <div class="container">
    <header class="header clearfix">
      <nav>
        <ul class="top_menu clearfix">
         <li><a href="/home">HOME</a></li>
         <li><a href="/diaries">ツイート一覧</a></li>
         <li><a href="/diaries/new">新規ツイート</a></li>
        </ul>
      </nav>
    </header>
      <div class="about">
          <h3>ツイートを送信する。</h3>
            <%= form_with(model: @diary, url: diaries_path, local: true) do |form| %>
              <%= form.hidden_field :content %>
          <div class="tweet_btn">
　　　　　    <p>内容:<%= @diary.content %></p>
        　 　 <%= form.submit  "ツイートする" %>
            <% end %>
          </div>
          
          <div class="back_btn">
　　　　    <%= form_with(model: @diary, url: new_diary_path, local: true, method: 'get') do |form| %>
　　　　    <%= form.hidden_field :content %>
            <%= form.submit 'もどる', name: 'back' %>
　　　　    <% end %>
　　　　  </div>
      </div>
    <footer class="footer">
        <p>DIVE INTO CODE test application</p>
    </footer>
    </div>
  </body>
</html>